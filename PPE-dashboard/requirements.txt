streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.24.0
plotly>=5.15.0
opencv-python>=4.8.0
Pillow>=9.0.0
python-dateutil>=2.8.0
ultralytics>=8.1.0
# NOTE: ultralytics requires a compatible torch installation. Install torch separately depending on your CUDA setup.
# Example (CUDA 11.8):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# Example (CPU-only):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu